# Instructions for working with the codebase in this exercise

- for testing and development, always use the prepared conda environment specified in `environment.yml`
- do not modify the `input/` folder, it contains the original figure and task context
- do not modify the main exercise `README.md`
